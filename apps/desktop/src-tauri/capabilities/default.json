{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core APIs (events) for the Spotlight and Settings windows. App commands are allowed by default.",
  "windows": ["spotlight", "settings"],
  "permissions": ["core:default"]
}
