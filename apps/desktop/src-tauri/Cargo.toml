[package]
name = "yacs-desktop"
description = "YACS desktop app"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

# A lib target keeps the door open for Tauri mobile (v2), which needs one.
[lib]
name = "yacs_desktop_lib"
crate-type = ["rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
gethostname = "1.1.0"
hex = "0.4.3"
keyring = { version = "3", features = ["apple-native", "windows-native"] }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
tauri = { version = "2", features = ["tray-icon", "macos-private-api", "image-png"] }
tauri-plugin-autostart = "2"
tauri-plugin-global-shortcut = "2"
tauri-plugin-single-instance = "2"
thiserror = "2.0.20"
tracing = "0.1.44"
tracing-subscriber = { version = "0.3.23", features = ["env-filter"] }
yacs-client = { version = "0.1.0", path = "../../../crates/yacs-client" }
yacs-core = { version = "0.1.0", path = "../../../crates/yacs-core" }

[dev-dependencies]
clap = "4.6.7"
tempfile = "3.27.0"
tokio = { version = "1.53.1", features = ["macros", "rt-multi-thread", "net"] }
yacs-server = { path = "../../../crates/yacs-server" }
